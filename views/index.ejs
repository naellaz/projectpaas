<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>💖 Data Pengguna Lucu 💖</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* 🌸 RESET DAN FONT */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: linear-gradient(180deg, #ffeefc, #f7f2ff);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 30px;
      color: #444;
      overflow-x: hidden;
    }

    /* 🌟 ANIMASI */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes floaty {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-6px); }
    }

    /* 🩷 WRAPPER */
    .container {
      width: 100%;
      max-width: 600px;
      background: #fff;
      border-radius: 25px;
      padding: 30px;
      box-shadow: 0 8px 25px rgba(255, 182, 193, 0.3);
      text-align: center;
      animation: fadeInUp 1.2s ease;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 20px;
      color: #ff7ab8;
      text-shadow: 1px 1px #ffd9e8;
      animation: floaty 3s ease-in-out infinite;
    }

    /* 🌷 FORM */
    form {
      background: #fff0fa;
      padding: 20px;
      border-radius: 20px;
      box-shadow: inset 0 0 10px rgba(255, 182, 193, 0.3);
      margin-bottom: 30px;
      animation: fadeIn 1.5s ease;
    }

    label {
      display: block;
      text-align: left;
      margin-bottom: 5px;
      font-weight: 600;
      color: #ff7ab8;
      font-size: 0.95rem;
    }

    input[type="text"],
    input[type="email"],
    input[type="file"] {
      width: 100%;
      padding: 10px 15px;
      margin-bottom: 15px;
      border: 2px solid #ffd1e8;
      border-radius: 15px;
      outline: none;
      transition: 0.3s;
      font-size: 0.95rem;
    }

    input:focus {
      border-color: #ff7ab8;
      box-shadow: 0 0 5px #ff7ab8;
    }

    button {
      background: #ff7ab8;
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 20px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: 0.3s;
      box-shadow: 0 4px 10px rgba(255, 122, 184, 0.4);
    }

    button:hover {
      background: #ff4b9d;
      transform: translateY(-2px);
    }

    /* 📜 DATA LIST */
    h2 {
      font-size: 1.5rem;
      color: #ff7ab8;
      margin-bottom: 15px;
      animation: fadeInUp 1.5s ease;
    }

    .user-card {
      background: #fff7fc;
      border: 2px dashed #ffd6ec;
      border-radius: 20px;
      padding: 15px;
      margin-bottom: 15px;
      text-align: center;
      transition: 0.3s;
      opacity: 0;
      animation: fadeInUp 0.8s ease forwards;
    }

    /* Delay animasi setiap card */
    .user-card:nth-child(1) { animation-delay: 0.2s; }
    .user-card:nth-child(2) { animation-delay: 0.4s; }
    .user-card:nth-child(3) { animation-delay: 0.6s; }
    .user-card:nth-child(4) { animation-delay: 0.8s; }

    .user-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 4px 15px rgba(255, 182, 193, 0.3);
    }

    .user-card img {
      width: 90px;
      height: 90px;
      object-fit: cover;
      border-radius: 50%;
      margin-bottom: 10px;
      border: 3px solid #ff7ab8;
    }

    .user-card strong {
      font-size: 1.2rem;
      color: #ff4b9d;
    }

    .empty {
      color: #aaa;
      font-style: italic;
      font-size: 0.95rem;
      animation: fadeIn 1.2s ease;
    }

    footer {
      margin-top: 20px;
      font-size: 0.85rem;
      color: #888;
      animation: fadeIn 1.8s ease;
    }

    footer a {
      color: #ff7ab8;
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🌸 Tambah Pengguna 🌸</h1>

    <form action="/submit" method="POST" enctype="multipart/form-data">
      <label>Nama:</label>
      <input type="text" name="name" placeholder="Masukkan nama lengkapmu..." required>

      <label>Email:</label>
      <input type="email" name="email" placeholder="contoh@email.com" required>

      <label>Foto Profil:</label>
      <input type="file" name="photo" accept="image/*">

      <button type="submit">✨ Simpan ✨</button>
    </form>

    <h2>📜 Data Tersimpan 📜</h2>

    <% if (users.length === 0) { %>
      <p class="empty">Belum ada pengguna yang tersimpan 🐣</p>
    <% } else { %>
      <% users.forEach(user => { %>
        <div class="user-card">
          <% if (user.photo) { %>
            <img src="/storage/<%= user.photo %>" alt="Foto Profil">
          <% } else { %>
            <img src="https://cdn-icons-png.flaticon.com/512/1077/1077114.png" alt="Default">
          <% } %>
          <p><strong><%= user.name %></strong></p>
          <p><%= user.email %></p>
        </div>
      <% }) %>
    <% } %>

    <footer>
      🌷 Dibuat dengan cinta oleh <a href="#">Node.js + PostgreSQL</a> 🌷
    </footer>
  </div>
</body>
</html>
